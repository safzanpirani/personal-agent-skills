interface:
  display_name: "/review-codex-auto"
  short_description: "Run a second headless Codex instance to review and fix current changes"
  default_prompt: "Use $review-codex-auto to generate a tailored current-branch review prompt, run it with codex exec in the current repository, and report the second Codex instance's final output."
policy:
  allow_implicit_invocation: false

