interface:
  display_name: "/review-codex-auto"
  short_description: "Run a live-steerable Codex review and fixes"
  default_prompt: "Use $review-codex-auto to run a live-steerable Codex Rudder review of the current changes, apply confident fixes, and report the verified result."
policy:
  allow_implicit_invocation: false
